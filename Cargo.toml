[workspace]
members = ["core/rs/temporaless"]
resolver = "3"

[workspace.package]
version = "0.8.0"

# The workspace root owns profiles; member-level profiles are ignored by
# Cargo. Keep release codegen optimized for protobuf-heavy I/O workloads.
[profile.release]
lto = "thin"
codegen-units = 1
